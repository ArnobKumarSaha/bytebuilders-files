apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: mg-restore
  namespace: demo
spec:
  driver: Restic
  repository:
    name: gcs-repo
  target:
    ref:
      apiVersion: appcatalog.appscode.com/v1alpha1
      kind: AppBinding
      name: mg-restore
    rules:
    - snapshots:
      - latest


# Error creating: pods "scanner-init-trivydb-cache-grkj5" is forbidden: violates PodSecurity "restricted:latest": 
# allowPrivilegeEscalation != false (container "starter" must set securityContext.allowPrivilegeEscalation=false), 
# unrestricted capabilities (container "starter" must set securityContext.capabilities.drop=["ALL"]), 
# runAsNonRoot != true (pod or container "starter" must set securityContext.runAsNonRoot=true), 
# seccompProfile (pod or container "starter" must set securityContext.seccompProfile.type to "RuntimeDefault" or "Localhost")